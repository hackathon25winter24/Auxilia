syntax = "proto3";

package user;

option go_package = "auxilia/pb";
option csharp_namespace = "Game.Network";

service UserService {
  // ID(UUID文字列)で検索
  rpc GetUser (GetUserRequest) returns (UserResponse);
  
  // 名前で検索
  rpc GetUserByName (NameRequest) returns (UserResponse);
  
  // ユーザー作成
  rpc CreateUser (CreateUserRequest) returns (UserResponse);
  
  // ユーザー情報の更新
  rpc UpdateUser (UpdateUserRequest) returns (UserResponse);
  
  // 全ユーザー取得
  rpc ListUsers(ListUsersRequest) returns (ListUsersResponse);
  
  // Hashを使ってログインする
  rpc Login(LoginRequest) returns (UserResponse);

  rpc DeleteUser(DeleteUserRequest) returns (DeleteUserResponse);
}

// --- メッセージ定義 ---

message ListUsersRequest {}

message ListUsersResponse {
  repeated UserResponse users = 1;
}

message LoginRequest {
  string name = 1;
  string password =2;
}

message NameRequest {
  string name = 1;
}

message GetUserRequest {
  string id = 1; // UUID
}

message CreateUserRequest {
  string name = 1;
  string password = 2;
}

// 更新用：IDで対象を指定し、その他の値を書き換える
message UpdateUserRequest {
  string id = 1;
  string name = 2;
  string password = 3;
  int32 story = 4;
  int32 num_wins = 5;
  int32 num_battles = 6;
  int32 rate = 7;
  int32 home_character_id = 8;
  int32 deck1 = 9; // 初期値は-1で、未設定を表します。
  int32 deck2 = 10;
  int32 deck3 = 11;
}

message UserResponse {
  string id = 1;
  string name = 2;
  int32 story = 3;
  int32 num_wins = 4;
  int32 num_battles = 5;
  int32 rate = 6;
  int32 home_character_id = 7;
  int32 deck1 = 8;
  int32 deck2 = 9;
  int32 deck3 = 10;
}

message DeleteUserRequest {
  string id = 1;
}

message DeleteUserResponse {
  bool success = 1;
}